# Get_Next_Line
The get_next_line function reads from a file descriptor and returns one line at a time, including the newline character (if present). Successive calls to the function should return the next line each time, until the end of the file is reached.
